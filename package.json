{
  "name": "devloop",
  "private": true,
  "description": "The bilingual antidote to AI developer anxiety: capture AI-driven requirements into living PRDs, verification evidence, and release confidence.",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest --workspace vitest.workspace.ts run",
    "lint": "pnpm --filter @devloop/core exec tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "vitest": "^3.2.0"
  }
}
